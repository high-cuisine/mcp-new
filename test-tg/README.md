# Test-TG Project

Проект состоит из двух независимых компонентов:
1. **Telegram бот** - отправляет скриптовое сообщение
2. **Веб-сайт** - заглушка с формой обратной связи

## 🤖 Telegram Бот

Простой бот, который отправляет сообщение о входе пользователя в систему.

### Запуск бота отдельно:
```bash
npm run start:bot
```

### Что делает:
- Отправляет сообщение: `пользователь 443е34 пошел в систему с паролем 123345`
- Работает в фоновом режиме

### Конфигурация:
- **BOT_TOKEN**: `7600472632:AAEbV7PF4lhmvIlIUnskoN72f4uy_2lkJ0Y`
- **CHAT_ID**: `7260594642`

---

## 🌐 Веб-сайт (Заглушка)

Простая форма обратной связи с красивым UI.

### Запуск сайта отдельно:
```bash
npm run start:website
```

### Доступ:
Откройте в браузере: `http://localhost:3000`

### Поля формы:
- 👤 Имя
- 📧 Email
- ⭐ Оценка (1-5 звезд)
- 💬 Комментарий

### Что делает:
- Показывает красивый alert после отправки
- Данные логируются в консоль
- **НЕ отправляет данные в Telegram** (это заглушка)

---

## 🐳 Запуск через Docker

### Запустить оба сервиса:
```bash
docker-compose up -d --build
```

### Запустить только бота:
```bash
docker-compose up -d telegram-bot
```

### Запустить только сайт:
```bash
docker-compose up -d website
```

### Остановить все:
```bash
docker-compose down
```

### Посмотреть логи:
```bash
# Логи бота
docker logs test-tg-bot

# Логи сайта
docker logs test-tg-website
```

---

## 📦 Установка зависимостей

```bash
npm install
```

## 📂 Структура проекта

```
test-tg/
├── index.js           # Telegram бот
├── website.js         # Веб-сервер (заглушка)
├── package.json       # Зависимости
├── Dockerfile         # Docker конфигурация
├── docker-compose.yml # Оркестрация контейнеров
└── public/            # Статические файлы сайта
    ├── index.html     # HTML форма
    ├── style.css      # Стили
    └── script.js      # JavaScript логика
```

